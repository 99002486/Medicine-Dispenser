#ifndef __MYDELAY_H__
#define __MYDELAY_H_

#include <stdio.h>
#include <time.h>

/**
@function-delay
@param-int seconds
@return-void
@description - Executes a delay for number of seconds passed as the parameter
**/

void delay(int seconds);

#endif
